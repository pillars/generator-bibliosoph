{
    "site": {
        "name"          : "<%= name %>"
      , "credits"       : <%= (domain_name) ? 'true' : 'false' %><%if (domain_name) { %>
      , "domain"        : "<%= domain_name %>"<% } %><%if (analytics_id) { %>
      , "analytics_id"  : "<%= analytics_id %>"<% } %><%if (repository) { %>
      , "repository"    : "<%= repository %>"<% } %><%if (download) { %>
      , "download"      : "<%= download %>"<% } %><%if (menu) { %>
      , "menu"          : []<% } %>
    }
}